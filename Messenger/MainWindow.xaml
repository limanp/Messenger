<Window x:Class="Messenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messenger"
        xmlns:src="clr-namespace:Messenger"
        mc:Ignorable="d"
        Title="Messenger" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="x:Type vm:ChatViewModel">
        </DataTemplate>
    </Window.Resources>
    <Grid>
       
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- List of dialoge -->
        <ListBox Name="listDialog" Grid.Column="0">
            <ListBoxItem>Item1</ListBoxItem>
            <ListBoxItem>Item2</ListBoxItem>
            <ListBoxItem>Item3</ListBoxItem>
            <ListBoxItem>Item4</ListBoxItem>
            <ListBoxItem>Item5</ListBoxItem>
            <ListBoxItem>Item6</ListBoxItem>
        </ListBox>
        
        <!-- List messages history -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            
            
            <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto">
                <ItemsControl Margin="10" Name="icMessageList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                        <Grid Margin="0,0,0,50">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/> <!-- Message Avatar -->
                                <ColumnDefinition Width="Auto"/> <!-- Message Text -->
                            </Grid.ColumnDefinitions>

                            <Border BorderBrush="Black" BorderThickness="2"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding ContactName}"/>
                                <TextBlock Margin="20,0,0,0" Text="{Binding Text}"/>
                            </StackPanel>
                            
                        </Grid>
                    </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    
                </ItemsControl>
            </ScrollViewer>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Name="MessageTextBox" Grid.Column="0" TextWrapping="Wrap" AcceptsReturn="True" Background="Aqua" />
                <Button Grid.Column="1" Content="Send" Click="Send_Message"/>
            </Grid>
                
            
            
        </Grid>
  
    </Grid>
</Window>
